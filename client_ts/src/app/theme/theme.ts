import { createMuiTheme } from "@material-ui/core/styles";
import { pink, teal, yellow, blueGrey } from "@material-ui/core/colors";

export default createMuiTheme({
	palette: {
		primary: blueGrey,
		secondary: yellow
	}
});
